# app/services/effects/blur_effect.py

def apply_blur(frame, t, kernel_size, context):
    """
    Stub for the blur effect. Currently, this does nothing and returns the original frame.
    Parameters:
      - frame: The current frame (NumPy array)
      - t: Current time in seconds
      - kernel_size: Kernel size for the blur (unused)
      - context: Additional context (unused)
    Returns:
      - The original frame unchanged.
    """
    return frame
